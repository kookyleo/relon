@import("lib_with_private.relon", as="lib")
{
  check_public: lib.public
  // `lib.secret` is unreachable: the private field never enters
  // `lib`'s exported map, so this would fail with `VariableNotFound`.
}
