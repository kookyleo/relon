\* TLC config for capability-sandbox-spec-2026-05-23.tla
\*
\* Skeleton config: 给最小有限 CONSTANTS 让 spec 可被 TLC 接受。
\* 未来 RFC 触发完整 verification 时扩展 set 元素并替换 INV1-INV4 stub。

CONSTANTS
    CapBits        = {b1, b2}
    HostFnIds      = {h1, h2}
    Schemas        = {s1}
    Methods        = {m1, m2}
    SourceHashes   = {src1}
    PolicyValues   = {"Granted", "Denied", "Default"}

SPEC Spec

INVARIANT Invariants
