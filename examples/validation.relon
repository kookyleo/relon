{
    @ensure.int
    @ensure.at_least(1024)
    @ensure.at_most(65535)
    "port": 80,
    @ensure.string
    @ensure.one_of([
        "TCP",
        "UDP"
    ])
    "protocol": "TCP",
    "nested": {
        @ensure.bool
        "is_active": true
    }
}
