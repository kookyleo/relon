{
    @fn(x)
    abs: x < 0 ? -x: x,
    @fn(a, b)
    max: a > b ? a: b,
    @fn(a, b)
    min: a < b ? a: b,
    @fn(value, min_value, max_value)
    clamp: value < min_value ? min_value: (value > max_value ? max_value: value)
}
