{
    split(value, separator): string.split(value, separator),
    join(values, separator): string.join(values, separator),
    replace(value, from, to): string.replace(value, from, to),
    upper(value): string.upper(value),
    lower(value): string.lower(value),
    contains(value, needle): string.contains(value, needle)
}
