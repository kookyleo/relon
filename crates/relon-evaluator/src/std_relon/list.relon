{
    first(l): l[0],
    compact(l): [
        x for x in l if x != null
    ],
    contains(l, value): list.contains(l, value),
    map(l, f): [
        f(x) for x in l
    ],
    filter(l, f): [
        x for x in l if f(x)
    ]
}
