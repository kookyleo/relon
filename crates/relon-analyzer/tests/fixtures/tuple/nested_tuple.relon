// Tuples nest. The outer slot is `List<(Int, String)>` and each
// element is itself a tuple — per-position checking flows recursively
// into each pair.
// EXPECTED: 0 × StaticTypeMismatch.
{ List<(Int, String)> rows: [(1, "a"), (2, "b")] }
