// v1.8 tuple-position access: `pair.5` on a 2-tuple is out of
// range. Under strict mode the walker surfaces the failed step
// as `UnknownReferenceType`.
// EXPECTED: ≥ 1 × UnknownReferenceType on the `bad` reference.
{
  (Int, String) pair: [42, "hello"],
  Int bad: pair.5
}
