// A library whose closure body spreads an untyped param. Imported by
// `entry.relon` (strict by default); strict contagion makes the
// analyzer flag the inner `{ ...n }` whose source type is Any.
{ f: (n) => { ...n } }
