// A library whose closure body spreads an untyped param. The lib is
// directive-less, so it is strict on its own; the analyzer flags the
// inner `{ ...n }` whose source type is Any.
{ f: (n) => { ...n } }
