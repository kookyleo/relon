// Default-strict entry that imports a directive-less library with a
// silent-fallback shape. Strictness is file-local, so the lib is strict
// on its own and surfaces the spread-source-unknown diagnostic itself.
#import * from "./lib.relon"
{ x: 1 }
