// Default-strict entry that imports a library with a silent-fallback
// shape. The entry's strict mode propagates onto the lib via workspace
// contagion, surfacing the spread-source-unknown diagnostic.
#import * from "./lib.relon"
{ x: 1 }
