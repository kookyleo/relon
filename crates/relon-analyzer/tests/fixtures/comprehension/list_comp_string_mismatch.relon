// v1.5 forward: list comprehension produces `List<Int>` but the
// declared slot is `List<String>` — strict catches the mismatch
// statically.
// EXPECTED: ≥ 1 × StaticTypeMismatch.
#strict
{ List<String> doubled: [x * 2 for x in range(5)] }
