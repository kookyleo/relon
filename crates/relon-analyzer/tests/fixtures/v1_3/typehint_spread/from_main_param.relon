// v1.3: spread of a #main param under strict mode. The param itself
// has a typed schema, so an implicit derivation accepts it; an
// explicit `<Order>` hint is also fine.
// EXPECTED: no diagnostics.
#strict
#schema Order { Int id: *, Float total: * }
#main(Order o) -> Dict<String, Int>
{ ...<Order> o }
