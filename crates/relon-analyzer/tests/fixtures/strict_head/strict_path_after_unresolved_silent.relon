// A known stdlib name like `range` does NOT trigger the strict
// head-unresolved check (it isn't `UnresolvedReference` —
// `is_known_fn` saves it).
// EXPECTED: 0 x UnknownReferenceType, 0 x UnresolvedReference (for
// `range`).
#main(Int n) -> List<Int>
range(n)
