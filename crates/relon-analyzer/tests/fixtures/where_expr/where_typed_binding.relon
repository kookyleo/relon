// v1.5 forward: where-expression bindings can themselves carry type
// hints; inference propagates through the bindings just like any
// dict literal.
// EXPECTED: no diagnostics.
#main(Int x) -> Int
(n + 1) where { Int n: x }
