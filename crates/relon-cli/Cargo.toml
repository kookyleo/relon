[package]
name = "relon-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Command line interface for the Relon language"
keywords = ["cli", "config", "dsl", "json", "sandboxed"]
categories = ["command-line-utilities", "config"]

[dependencies]
# The CLI ships both backends so `--backend wasm-aot` is always
# available. The library facade keeps the wasm-AOT bridge gated
# behind the matching feature so `relon-wasm` (browser playground)
# stays clean of wasmtime.
relon = { workspace = true, features = ["wasm-aot"] }
relon-parser = { workspace = true }
relon-evaluator = { workspace = true }
relon-eval-api = { workspace = true }
relon-analyzer = { workspace = true }
relon-codegen-wasm = { workspace = true }
relon-fmt = { workspace = true }
relon-lsp = { workspace = true }
clap = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
anyhow = { workspace = true }
miette = { workspace = true, features = ["derive", "fancy"] }
ariadne = "0.6.0"
